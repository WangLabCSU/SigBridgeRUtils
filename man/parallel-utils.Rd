% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/12_furrr.R
\name{parallel-utils}
\alias{parallel-utils}
\alias{availableCores}
\alias{availableWorkers}
\title{Parallel Processing Utilities}
\usage{
availableCores()

availableWorkers()
}
\value{
An integer specifying the number of available CPU cores.

A character vector of available worker identifiers. The specific
format depends on the parallel backend and system configuration.
}
\description{
Functions for querying available computational resources in parallel processing environments.
These functions provide information about available CPU cores and workers for
configuring parallel execution in the SigBridgeR package.

availableCores returns the number of available CPU cores for parallel processing.
This function detects various system settings and environment variables that
may limit the number of cores available for computation.

availableWorkers returns a character vector of available worker identifiers
for parallel processing. This is useful for setting up cluster configurations
and distributed computing.
}
\details{
The function considers several factors when determining available cores:
\itemize{
\item System configuration (physical and logical cores)
\item Environment variables (e.g., R_FUTURE_AVAILABLECORES_FALLBACK)
\item R options (e.g., future.availableCores.fallback)
\item Container and cloud environment limitations
}

The function detects available workers based on:
\itemize{
\item System hostnames for multi-machine clusters
\item Local machine core count for single-machine parallelization
\item Environment-specific worker configurations
\item Cloud and HPC environment settings
}
}
\examples{
\dontrun{
# Get number of available cores
n_cores <- availableCores()
print(n_cores)

# Use in parallel configuration
plan("multisession", workers = availableCores() - 1)
}

\dontrun{
# Get available workers
workers <- availableWorkers()
print(workers)

# Use in cluster configuration
plan("cluster", workers = availableWorkers())
}

}
\seealso{
\code{\link[future:re-exports]{future::availableCores()}} for the underlying implementation,
\code{\link[=availableWorkers]{availableWorkers()}} for getting available worker identifiers

\code{\link[future:re-exports]{future::availableWorkers()}} for the underlying implementation,
\code{\link[=availableCores]{availableCores()}} for getting the number of available cores
}
